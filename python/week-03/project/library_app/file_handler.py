import csv
import json
import os


def load_library(filepath):
    #Loads the book list from a JSON file. Safely returns an empty list if the file is missing or corrupt.
    #return list of book if books exists else return empty list []
    
    return None

def save_library(filepath, books):
    #Saves the book list to a JSON file in a readable format (indent=4).

    return None

def export_to_csv(books, filepath):
    #Exports the book list in CSV format.

    return None

def import_from_csv(filepath):
    #Imports the book list in CSV format.
    #return list of book if books exists else return empty list []

    return None